<Window x:Class="_0202_Practixhka.PartnerMaterialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Материалы партнеров" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Список продукции для партнеров" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#FF546F94"/>

        <DataGrid Grid.Row="1" x:Name="produsctsGrid" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукт" Binding="{Binding product_name}" Width="500"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding quantity}" Width="100"/>
                <DataGridTextColumn Header="Цена за ед." Binding="{Binding patner_minimal_price, StringFormat=N2}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" x:Name="txtSummary" FontWeight="SemiBold" 
                   Margin="0,10,0,10" FontSize="12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Обновить" Click="RefreshButton_Click" 
                    Padding="10,5" Margin="0,0,10,0" Foreground="White" Background="#FF546F94"/>
            <Button Content="Назад" Click="BackButton_Click" 
                    Padding="10,5" Background="#FF546F94" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>